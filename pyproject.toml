[project]
name = "gensh"
version = "0.1.0"
description = "NL → shell command CLI using Grok"
requires-python = ">=3.10"

dependencies = [
  "typer",
  "rich",
  "google-generativeai"
]

[project.scripts]
gensh = "cli.main:app"

[tool.setuptools]
packages = ["cli", "core"]
